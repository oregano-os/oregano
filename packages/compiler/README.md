# compiler — files → technical runtime rules

**Trigger: approved unattended workflow.** The compiler remains intentionally
empty until the fail-closed capability catalog and resolver contract are
approved. This is a dependency gate, not the former general Stage 4 lock.

It will deterministically, without an LLM, produce an enforcement manifest from
workflow steps, scope declarations, Tool grants, connections, and Instance
availability.
