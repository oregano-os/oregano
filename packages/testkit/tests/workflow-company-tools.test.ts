// Business cases live with synthetic fixtures; their computations use the
// maintained Tool inspector and isolation, never a second workflow executor.
import "../fixtures/workflow-computations.test.ts";
