# Claude entrypoint

Read `AGENTS.md` and `docs/README.md` before making changes.

The canonical project documentation lives under `docs/`. This file is a stable
bootstrap only; it is not an architecture document, project status, build
brief, or product specification.

As an Agent Contributor, write all engineering artifacts in English and use the CompanyOS Workbench
commands described in `docs/workbench/overview.md`.
Use `docs/onboarding/` and `companyos onboard` when establishing or receiving a
Company Workspace.
