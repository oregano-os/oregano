# minimal-company — starter structure for a new company
**Trigger: pilot 2. Do NOT fill earlier.** DISTILLED from real Company Workspaces
(commonalities → template with guiding questions), not invented at a desk.

**eve parallel:** eve keeps `apps/fixtures` (sample agents for e2e, smoke
tests AND local dev) separate from `apps/templates`. Transferred: the sample
company from the dry run becomes our fixture (test data for testkit), this
template the cloning starting point — two roles, two places.
